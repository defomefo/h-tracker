flask>=3.0
flask-cors>=5.0
gunicorn>=23.0
google-genai>=1.0
python-dotenv>=1.0
# Postgres driver for production (Neon/Render). SQLite is the fallback for
# local dev when DATABASE_URL isn't set — sqlite3 is in the stdlib.
psycopg[binary]>=3.2
# PDF brief generation. WeasyPrint renders HTML+CSS → PDF with proper
# typography (kerning, ligatures, real text in the PDF — not raster).
# Requires Cairo + Pango at the OS level — see Dockerfile.
weasyprint>=63.0
# Jinja2 ships with Flask but we pin it explicitly because we depend on
# it directly for the brief template.
jinja2>=3.1
# Prospect Discovery — Tavily is purpose-built for AI grounding, returns
# clean web search results with content snippets. Used to feed Gemini
# verifiable sources so we never hallucinate institutions.
tavily-python>=0.5
# URL-alive checks (HEAD requests) for the hallucination defense + ROR API
# (Research Organization Registry) for university existence verification.
requests>=2.31
# Fuzzy matching for UNIS dedupe (layer 4 of the prospect-discovery
# hallucination defense). Same dependency as scripts/clean_sponsors.py.
rapidfuzz>=3.5
